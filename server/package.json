{
  "name": "unhookapp-server",
  "version": "1.0.0",
  "description": "UnhookApp backend — Express API for Gmail inbox cleanup",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node --test \"services/**/*.test.js\"",
    "lint": "node --check index.js && node --check routes/auth.js && node --check routes/emails.js && node --check db/init.js && node --check db/queries.js && node --check services/parser.js && node --check services/classifier.js && node --check services/gmail.js && node --check services/gmailHelper.js && node --check services/subscriptions.js && node --check services/crypto.js"
  },
  "keywords": [
    "gmail",
    "unsubscribe",
    "inbox",
    "cleanup"
  ],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "better-sqlite3": "^12.11.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^7.5.0",
    "express-session": "^1.19.0",
    "google-auth-library": "^10.7.0",
    "googleapis": "^173.0.0",
    "helmet": "^8.2.0"
  }
}
